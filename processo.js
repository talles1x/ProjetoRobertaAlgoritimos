/**
 * Representa um processo com um ID único e um tamanho de alocação.
 */
class Processo {
    constructor(id, tamanho) {
        this.id = id;
        this.tamanho = tamanho;
    }
}

module.exports = Processo;
